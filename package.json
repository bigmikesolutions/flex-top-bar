{
  "name": "wordpress-top-bar-e2e",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@vitest/ui": "^4.1.1",
    "@vue/test-utils": "^2.4.6",
    "happy-dom": "^20.8.8",
    "vitest": "^4.1.1"
  },
  "scripts": {
    "test": "npm test -w plugins/top-bar",
    "test:ui": "npm run test:ui -w plugins/top-bar",
    "test:e2e": "playwright test --workers=1 --timeout=30000",
    "test:e2e:single-bar": "playwright test --workers=1 e2e/top-bar-single.spec.ts",
    "test:e2e:multi-bar": "playwright test --workers=1 e2e/top-bar-multi.spec.ts",
    "test:e2e:headed": "playwright test --workers=1 --headed",
    "test:e2e:ui": "playwright test --workers=1 --ui"
  },
  "workspaces": [
    "plugins/top-bar"
  ],
  "dependencies": {
    "vue": "^3.5.31"
  }
}
